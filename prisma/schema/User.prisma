model User {
    id_user Int @id @default(autoincrement())
    email String @unique
    password String
    role String @default("muhafiz")
    // profile Profile?

    deleted_at DateTime?

    @@map("user")
}